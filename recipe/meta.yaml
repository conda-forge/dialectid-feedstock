{% set name = "dialectid" %}
{% set version = "0.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/dialectid-{{ version }}.tar.gz
  sha256: 8f924a8c986f09e981e76e21ae27cc0c8968a27cb7a1127cff85c13e75d6c533

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
    - scikit-learn
  run:
    - python >=3.6
    - scikit-learn
    - microtc
    - b4msa
    - evomsa

test:
  imports:
    - dialectid
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/dialectid/
  summary: Computational models for dialect identification.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mgraffg
